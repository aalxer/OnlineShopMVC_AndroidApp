<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".appActivites.managementActivities.OrdersManagementActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/ordersDisplay"
        android:layout_width="409dp"
        android:layout_height="729dp"
        android:paddingTop="?attr/actionBarSize"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />

    <TextView
        android:id="@+id/noOrdersText"
        android:layout_width="234dp"
        android:layout_height="158dp"
        android:text="There Are No Orders"
        android:fontFamily="sans-serif"
        android:textSize="8pt"
        android:textAlignment="center"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/ordersDisplay"
        app:layout_constraintStart_toStartOf="@+id/ordersDisplay"
        app:layout_constraintTop_toTopOf="@+id/ordersDisplay" />

</androidx.constraintlayout.widget.ConstraintLayout>